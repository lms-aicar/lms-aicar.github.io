(function(){const form=document.querySelector('#pretest-form'),status=document.querySelector('#pretest-status'),course=document.querySelector('#pretest-course');function call(a,d){return LMS_API.call(a,d,true)};(async function(){try{const d=await call('COURSE_EDITOR_COURSES',{});d.courses.forEach(function(c){const o=document.createElement('option');o.value=c.courseId;o.textContent=c.title;course.append(o)})}catch(e){status.textContent=e.message}})();form.addEventListener('submit',async function(e){e.preventDefault();const v=new FormData(form);try{await call('PRETEST_CREATE',{courseId:v.get('courseId'),title:v.get('title'),questionCount:Number(v.get('questionCount')),timeLimitMinutes:Number(v.get('timeLimitMinutes')),maxAttempts:Number(v.get('maxAttempts')),passingScore:Number(v.get('passingScore'))});status.textContent='เปิดใช้ Pre-test แล้ว';status.dataset.state='success'}catch(err){status.textContent=err.message}})})();
