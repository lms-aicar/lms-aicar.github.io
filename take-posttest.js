(function(){const status=document.querySelector('#status'),course=document.querySelector('#course'),start=document.querySelector('#start'),go=document.querySelector('#go'),form=document.querySelector('#form'),questions=document.querySelector('#questions');let attempt;(async function(){try{const data=await LMS_API.call('POSTTEST_AVAILABLE',{},true);if(!data.courses.length){status.textContent='ยังไม่มี Post-test ที่เปิดให้ทำ';return;}data.courses.forEach(function(item){const option=document.createElement('option');option.value=item.courseId;option.textContent=item.title;course.appendChild(option);});start.hidden=false;}catch(error){status.textContent=error.message;}}());go.addEventListener('click',async function(){try{attempt=await LMS_API.call('POSTTEST_START',{courseId:course.value},true);document.querySelector('#title').textContent=attempt.title;attempt.questions.forEach(function(q,i){const field=document.createElement('fieldset'),legend=document.createElement('legend');legend.textContent=(i+1)+'. '+q.prompt;field.appendChild(legend);q.choices.forEach(function(choice,index){const label=document.createElement('label'),radio=document.createElement('input');radio.type='radio';radio.name=q.questionId;radio.value=index;radio.required=true;label.append(radio,' ',choice);field.appendChild(label);});questions.appendChild(field);});start.hidden=true;form.hidden=false;}catch(error){status.textContent=error.message;}});form.addEventListener('submit',async function(event){event.preventDefault();try{const values=new FormData(form),answers=attempt.questions.map(function(q){return{questionId:q.questionId,choiceIndex:Number(values.get(q.questionId))};}),result=await LMS_API.call('POSTTEST_SUBMIT',{attemptId:attempt.attemptId,answers:answers},true);status.textContent='คะแนน '+result.score+'%';form.hidden=true;}catch(error){status.textContent=error.message;}});}());
