(function(){const form=document.querySelector('#content-form'),status=document.querySelector('#content-status'),select=document.querySelector('#lesson-id');function call(a,d){return LMS_API.call(a,d,true)};(async function(){try{const d=await call('COURSE_EDITOR_LESSONS',{});d.lessons.forEach(function(l){const o=document.createElement('option');o.value=l.lessonId;o.textContent=l.courseTitle+' / '+l.unitTitle+' / '+l.title;select.append(o)})}catch(e){status.textContent=e.message}})();form.addEventListener('submit',async function(e){e.preventDefault();const v=new FormData(form);try{await call('LESSON_CONTENT_CREATE',{lessonId:v.get('lessonId'),contentType:v.get('contentType'),body:v.get('body'),mediaUrl:v.get('mediaUrl'),sortOrder:Number(v.get('sortOrder'))});status.textContent='เพิ่มเนื้อหาแล้ว';status.dataset.state='success';form.reset()}catch(err){status.textContent=err.message}})})();
